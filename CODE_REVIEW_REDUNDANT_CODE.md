# Redundant / Dead / Obsolete Code Review

Generated by Codex review on 2026-04-24.

## High Severity

- `app/api/setup/route.ts:1-244`
  Why it looks redundant/useless: this is a second household-creation backend that is not referenced by the current app flow. The live onboarding UI posts to `app/api/onboarding/household/route.ts:91-272`, while `/api/setup` is only mentioned in `README.md:278`. It also creates a local password-backed user (`password`, `bcrypt`) that does not match the Cognito-based onboarding path.
  Confidence: High
  Suggested cleanup: remove the route entirely, or explicitly migrate current callers to it and delete `app/api/onboarding/household/route.ts`.

- `app/api/telegram/bindings/complete/route.ts:1-278`
  Why it looks redundant/useless: this route is unreferenced by app code and appears to duplicate webhook-driven binding completion already implemented in `app/api/telegram/webhook/route.ts:322-425`. The duplicate path repeats group-conflict checks, binding activation, onboarding completion, and secret validation. Repo-wide search only found `/api/telegram/bindings/complete` in `README.md:280`.
  Confidence: High
  Suggested cleanup: delete the route, or make it the single activation path and have the webhook call into shared logic.

## Medium Severity

- `app/api/telegram/bindings/bootstrap/route.ts:1-146`
  Why it looks redundant/useless: current onboarding pages do not call this endpoint. `app/onboarding/connect-telegram/page.tsx:28-35` reads `bindingId`, `onboardingToken`, and `deepLink` directly from the URL, and repo search found no runtime reference to `/api/telegram/bindings/bootstrap` outside `README.md:279`.
  Confidence: High
  Suggested cleanup: remove the endpoint, or change onboarding to bootstrap from the server and then delete the current querystring-only path.

- `app/api/reminders/fire/route.ts:20-97`
  Why it looks redundant/useless: the endpoint self-identifies as `legacy_fallback_endpoint`, and the primary reminder architecture already uses `app/api/reminders/process-queue/route.ts` plus SQS consumption. Repo references only show docs plus this file.
  Confidence: High
  Suggested cleanup: remove after confirming no external cron/manual operator still calls it; otherwise quarantine it behind an internal-only ops namespace.

- `app/api/reminders/process-due/route.ts:21-39`, `lib/tasks/process-due-reminders.ts:15-84`
  Why it looks redundant/useless: this is a second reminder delivery mechanism that bypasses the SQS queue path described in `README.md:167-177`. Repo-wide usage only points to the route itself and docs. It also overlaps with claim/delivery logic already used from queue consumption.
  Confidence: Medium
  Suggested cleanup: delete if SQS/EventBridge is the committed architecture; otherwise document it as the supported fallback and consolidate shared ops behavior.

- `lib/aws/reminder-scheduler.ts:149-176`, `prisma/schema.prisma:222-224`
  Why it looks redundant/useless: `deleteReminderSchedule` has no callers, and the persisted cancellation-oriented fields `Reminder.scheduleName`, `scheduledBy`, and `canceledAt` are only written, not used to drive any live cleanup path. Today the code can create schedules but not actually consume those fields to cancel or reconcile them.
  Confidence: Medium
  Suggested cleanup: either implement real cancel/resync flows that use these fields, or remove the unused delete helper and defer the extra columns until cancellation exists.

## Low Severity

- `app/api/setup/route.ts:18-99`, `app/api/onboarding/household/route.ts:13-89`, `app/api/telegram/bindings/bootstrap/route.ts:8-44`, `app/api/telegram/bindings/status/route.ts:6-38`, `app/api/dashboard/route.ts:11-31`, `app/api/household/route.ts:10-58`
  Why it looks redundant/useless: repeated `jsonError`, string normalization, validation, and Telegram deep-link construction logic is copied across routes rather than shared. This is not dead code, but it is duplicated enough to drift.
  Confidence: High
  Suggested cleanup: extract small shared route helpers for JSON errors, normalized inputs, and Telegram deep-link generation.

- `app/api/telegram/bindings/bootstrap/route.ts:34-44`, `app/api/telegram/bindings/status/route.ts:28-38`, `lib/dashboard.ts:19-48`
  Why it looks redundant/useless: onboarding/binding state derivation exists in three separate implementations (`mapOnboardingStatus`, `deriveFrontendState`, `deriveDashboardConnectionState`) with overlapping but not identical rules. That duplication is easy to forget when statuses change.
  Confidence: High
  Suggested cleanup: centralize binding/onboarding state derivation in one shared module and make API routes consume it.

- `prisma/schema.prisma:9-34`, `prisma/schema.prisma:74-76`, `prisma/schema.prisma:111`, `prisma/schema.prisma:131`, `prisma/schema.prisma:225`
  Why it looks redundant/useless: several schema abstractions currently only encode one real option: `UserRole` only has `OWNER`, `MessagingPlatform` only has `TELEGRAM`, `ReminderClaimStatus` only has `CLAIMED`, and `Assistant.runtimeType` defaults to `"openclaw"` everywhere. These may be future-facing, but right now they add branching surface without active variability.
  Confidence: Medium
  Suggested cleanup: keep only if near-term multi-role/multi-platform support is planned; otherwise simplify to booleans/strings/defaults and reintroduce enums when a second value exists.

## Verification Notes

- Codex traced usages with repo-wide search and source inspection.
- Codex attempted `eslint`, but it failed in its environment with `SecItemCopyMatching failed -50`, so the review is source-inspection based rather than lint-backed.
- The Codex session itself ran read-only and could not write this file directly; this file was saved afterward from Codex's completed report.
